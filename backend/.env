# 数据库选择：设置为 true 使用 SQLite（无需安装 MySQL），设置为 false 使用 MySQL
USE_SQLITE=true

# MySQL 数据库连接配置（USE_SQLITE=false 时使用）
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/animal_rescue?charset=utf8mb4

# 服务配置
HOST=0.0.0.0
PORT=8000
